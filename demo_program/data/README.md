# Data

Generated captures go here by default.

```text
raw/        raw binary mmWave packets
processed/  CSV files decoded from packets
plots/      saved visual debug images
```

The existing root-level CSV/bin files are old captures. New captures from `capture.py` default to this directory.
